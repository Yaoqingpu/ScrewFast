# Cloudflare Pages — static Astro site
# Dashboard: Workers & Pages → Create → Connect to Git → this repo
# Or CLI: pnpm build && npx wrangler pages deploy dist --project-name=ferrulex

name = "ferrulex"
compatibility_date = "2026-09-23"
pages_build_output_dir = "dist"
